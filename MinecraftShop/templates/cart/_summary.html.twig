<div class="cart-summary">
    <i class="fas fa-shopping-cart"></i>
    <span class="item-count">{{ itemCount }}</span> item(s)
    <span class="total">{{ total }} diamonds</span>
    <a href="{{ path('app_cart_index') }}" class="btn btn-sm btn-primary">View Cart</a>
</div>